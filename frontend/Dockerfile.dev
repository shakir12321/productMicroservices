# Development Dockerfile - Much faster rebuilds
FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci

# Copy source code
COPY . .

# Expose port
EXPOSE 3000

# Start in development mode (much faster)
CMD ["npm", "run", "dev"]

